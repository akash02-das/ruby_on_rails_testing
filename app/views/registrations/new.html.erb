<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card mt-5">
      <div class="card-header">
        <h1>Sign Up</h1>
      </div>
      <div class="card-body">
      <%= form_with model: @user, url: signup_path do |form| %>
        <div class="mb-3">
          <%= form.label :email %>
          <%= form.email_field :email, class: 'form-control', placeholder: 'Enter a valid email' %>
        </div>
        <div class="mb-3">
          <%= form.label :password %>
          <%= form.password_field :password, class: 'form-control', placeholder: 'Enter your password' %>
        </div>
        <div class="mb-3">
          <%= form.label :password_confirmation %>
          <%= form.password_field :password_confirmation, class: 'form-control', placeholder: 'Enter your confirm password' %>
        </div>
          <div class="mb-3">
            <%= form.submit 'Sign Up', class: 'btn btn-primary' %>
          </div>
      <% end %>
      </div>
    </div>
  </div>
</div>
